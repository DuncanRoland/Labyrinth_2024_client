"use strict"

document.addEventListener("DOMContentLoaded", initGameListPage);

function initGameListPage() {
    findGameId();
    joinGame();

}

function findGameId(event) {
    event.preventDefault();
    const $findGameIdInput = document.querySelector("#findGameId");
    $findGameIdInput.value;
}

function joinGame() {

}